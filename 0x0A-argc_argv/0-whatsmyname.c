#include <stdlib.h>
#include <stdio.h>

/**
 * main- a program that prints its name , new line
 *
 * @argc: count args
 * @argv: args
 *
 * Return: Always 0 (Success)
*/

int main(int __attribute__((unused)) argc, char *argv[])
{
	int j = 0;

	printf("%s\n", argv[j]);
	return (0);
}
